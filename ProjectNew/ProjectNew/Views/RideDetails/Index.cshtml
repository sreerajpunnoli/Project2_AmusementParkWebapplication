@model IEnumerable<ProjectNew.Models.RideDetail>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Availibility of Rides as per Days</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            <text style="color:black">
                @Html.DisplayNameFor(model => model.RideDay)
            </text>
        </th>
        <th>
            <text style="color:black">
                @Html.DisplayNameFor(model => model.RideSeats)
            </text>
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            <text style="color:black">
                @Html.DisplayFor(modelItem => item.RideDay)
            </text>
        </td>
        <td>
            <text style="color:black">
                @Html.DisplayFor(modelItem => item.RideSeats)
            </text>
        </td>
        <td>
            @Html.ActionLink("Sign In & Book", "SignIn", "Registration", new { area = "" }, new { @class = "btn" })
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>*@
    </tr>
}

</table>
